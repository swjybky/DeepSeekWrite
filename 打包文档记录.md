# Write Claw 便携包打包说明（PyInstaller · Windows onedir）

本文记录 **绿色免安装目录包** 的完整流程（方案 A）：打好的 `dist/WriteClaw` 整文件夹压缩成 ZIP 给他人，解压后运行 `WriteClaw.exe`。

---

## 一、你给别人的机器需要什么

| 依赖 | 说明 |
|------|------|
| **WebView2** | [Microsoft Edge WebView2 Runtime](https://developer.microsoft.com/microsoft-edge/webview2/)（Evergreen）。未安装时常 **白屏**，README 亦有说明。 |
| **解压目录可写** | 书架数据、偏好与提示词覆盖写在 **`WriteClaw.exe` 同级目录下的 `.data`**，请勿把解压目录设在只读位置。 |

**不需要**：Python、Node.js（仅在**你电脑上**打包时需要）。

---

## 二、你本机打包环境

- Python **3.10+**（与项目一致即可）
- 已创建并激活虚拟环境（推荐）：`python -m venv .venv` → `.venv\Scripts\activate`
- Node.js **18+**、`npm`

**必读**：打包用的 Python 环境必须先安装 **`requirements.txt`**（含 **pywebview**）。若仅用 `pip install pyinstaller` 而未装依赖，会得到 **缺失 `webview` 模块的损坏 exe**（无法正常启动）。

```powershell
pip install -r requirements.txt
pip install pyinstaller
```

---

## 三、标准打包命令（在项目根目录 `write-claw`）

前端构建（产出 `web/dist/`，缺一无法打包，`packaging\WriteClaw.spec` 会主动报错退出）：

```powershell
cd web
npm install
npm run build
cd ..
```

执行 PyInstaller（仍在**项目根目录**）：

```powershell
pyinstaller packaging\WriteClaw.spec
```

成功后的输出目录：

```
dist\WriteClaw\
  WriteClaw.exe       # 用户双击入口
  _internal\          # 依赖与嵌入资源（勿删）
  .data\              #（用户首次运行后自动生成）书架等数据
```

将 **`dist\WriteClaw` 整个文件夹** 打成 ZIP（或 7z）分发；对方解压至任意路径即可使用。

**不要运行 `build` 目录里的 exe：** `build\WriteClaw\` 仅是构建中间产物，`WriteClaw.exe` 旁**没有**完整的 `_internal`（无 `python312.dll`），会出现「Failed to load Python DLL / 找不到指定的模块」。请务必使用 **`dist\WriteClaw\WriteClaw.exe`**。

---

## 四、冻结版路径约定（已实现）

源码与便携包共用一套逻辑（`app/runtime_paths.py`）：

| 类型 | 说明 |
|------|------|
| **只读资源** | `web/dist`、`app/assets`、`app/prompt_defaults` 来自打包时的 `_internal`（`sys._MEIPASS`）。 |
| **可写数据** | `.data`（`books.json`、`preferences.json`、`prompt_overrides/…`）在 **`WriteClaw.exe` 所在目录**，避免写入 `Program Files` 无权或单次 exe 解压临时目录导致丢数据。 |
| **AI 默认环境文件**（可选） | 按顺序读取**第一个存在的**文件：`exe` 旁的 `.env`、`.deepseek.env`、`.kimi.env`，以及包内 `_internal/app/` 下同名文件（打包时 **默认不包含**这些文件，密钥请放 `exe` 旁自行分发）。不要把含密钥的 env 打进**公开**分发包。 |

---

## 五、调试

- **开发白屏**：`WRITECLAW_DEBUG=1` 再启动可查前端控制台。
- **便携 exe 控制台**：默认 `console=False`（无黑框）。若在 `packaging\WriteClaw.spec` 里把 `EXE(…, console=False, …)` 改为 `console=True`，可看到 stderr 报错，便于单次排错后再改回。

---

## 六、体积与耗时说明（参考）

当前 spec 会通过 pywebview/hook **连同 PySide6 等一并收集**，Windows 便携目录体积常见为 **数百 MB**（量级约 **500MB+**），打包耗时约 **几分钟**。若仅需 Windows 分发、希望极限瘦身，需单独评估剔除 Qt 链路（风险与工作量较大），本文档不展开。

---

## 七、相关文件

| 路径 | 作用 |
|------|------|
| `packaging/pyi_entry.py` | PyInstaller 入口：`from app.main import main`。 |
| `packaging/WriteClaw.spec` | onedir 配置、`web/dist` 与提示词模板等 datas。 |
| `app/runtime_paths.py` | `bundle_root()` / `writable_root()`，`sys.frozen` 分支。 |

---

## 八、自检清单（发给别人前）

- [ ] `web/dist/index.html` 存在且为新构建。
- [ ] 已在**同一 Python 环境**执行 `pip install -r requirements.txt` 再打 PyInstaller。
- [ ] `pyinstaller packaging\WriteClaw.spec` 无报错，`dist\WriteClaw\WriteClaw.exe` 本地能打开。
- [ ] ZIP 里是 **整个 WriteClaw 文件夹**（含 `_internal`），不是只打包单个 exe。
- [ ] （可选）在压缩包旁的说明里提醒安装 **WebView2**。

---

*文档随仓库维护；若更改 spec 或路径逻辑，请同步更新本节。*
